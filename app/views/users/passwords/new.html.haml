.login-box
  .login-logo
    = t("user.recover_password.title")
  .login-box-body
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      = devise_error_messages!
      .form-group
        = f.email_field :email, :class => "form-control", :placeholder => t("user.recover_password.email_field"), autofocus: true
      .footer
        = f.submit t("user.recover_password.submit_field"), :class => "btn btn-block btn-primary btn-flat"
        = render "users/shared/links"
